/*
 * Mandatory:
 *
 * vars.volume.storage
 * vars.volume.class
 */

/*
 *
 */
aa-pvc-yaml(parent, vars) ::= <<
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: interscalar-com-1
  namespace: interscalar-com
  labels:
    group: interscalar-com
  annotations:
    volume.beta.kubernetes.io/storage-class: <vars.volume.class>
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: <vars.volume.storage>

>>
